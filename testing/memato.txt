P{
    @int A.B; no se si se debria poder declarar asi
    int A, B, C
    @ int A B, C;
    int A, B, ;
    int A, B
    int A
    int FUNCION (int A) {}
    int FUNCION (int A, cv int B) {}
    int  (int A, cv int B) {}
    int FUNCION (int A, int B) {}
    int A

    int FUNCION (int A) {
        return(3I + 6.5);
    }

    int FUNCION (int A) {
        return(3I + 6.5)
    }

    int FUNCION (int A) {
        return(3I + );
    }

    int FUNCION (int A) {
        return( + 6.5)
    }

    @int FUNCION (int A) {
    @    return(A B)
    @}

    int A := 3I + 4I;
    int A := 3I + 4I
    int A := 3I + 
    @int A := 3I 4I; 
    int A := + 4I; 
    int A  3I + 4I; 
    int  := 3I + 4I; 

    A := B;
    A := 3I;
    A := 4.5;
    A := 4.5D+6;
    A.C := 3I + 4I;
    A.C := 3I - 4I;
    A := 4 * 6.6;
    A := 4 / 6.6;
    A.C := 3I + ;
    A.C := 3I - ;
    A := 4 * ;
    A := 5 / ;
    A.C := + 3I ;
    A.C := - 3I ;
    A.C := - -3I ;
    A :=  * 4;
    A := / 5 ;
    @A := 4I 5I;
    @A := B 3I; 

    A.A = 1;

    A, B = 1;
    A.A, B.B, C.C = 1I, 2I, 3I;

    A, B = ;
    
    = 2I, 3I;

    A B = 2I, 3I;
    A, B = 2I 3I;

    A := 3I;

    if (A > 5I) {
        A := 3I + 3I;
    } else {
        B := 3I * 2;
    }
    endif;

    if (A < 5I) {
        A := 5;
    }
    endif;

    if (A == 5)
        A := 3I;
    endif
    
    if A == 5)
        A := 3I;
    endif;
    
    if (A == 5
        A := 3I;
    endif;
    
    if A == 5
        A := 3I;
    endif;

    if (A =! 5) 
    else
    endif;

    if (A =! 5) {}
    else
    endif;

    if () {

    }
    endif;

    if (A >) {

    }
    endif;

    if ( < B) {

    }
    endif;

    if ( A B) {

    }
    endif;

    if ( < B) {

    }

    if ( < B) {

    } else {
        
    }

    for (I from 1I to 10I) {
        B := A;
        A := A + 1;
    };

    for (I from 1I to 10I)
        A := A + 1;

    for I from 1I to 10I) {
        A := A + 1;
    };

    for ( from 1I to 10I) {
        A := A + 1;
    };

    for (I 1I to 10I) {
        A := A + 1;
    };

    for (I from to 10I) {
        A := A + 1;
    };

    for (I from 1I  10I) {
        A := A + 1;
    };

    for (I from 1I to ) {
        A := A + 1;
    }

    for (I from 1I to 10I {
        A := A + 1;
    }

    for I from 1I to 10I {
        A := A + 1;
    }

    for (I from 1I to 10I) {
        
    };
    
    for (I from 1I to 10I)

    print(8.5);
    print(8.5);
    print8.5);
    print();
    print(int A:=3I);
    print(8.5;

    SUMAR(4I -> A, 5I + 6.5 -> B);
    SUMAR(4I -> A, trunc(5I / 6I)-> B);
    SUMAR(4I -> A, trunc(5I / 6I-> B);
    SUMAR(4I -> A, trunc5I / 6I)-> B);
    SUMAR(4I -> A, trunc5I / 6I-> B);
    SUMAR(4I -> A, 5I + 6.5 -> );

    SUMAR(4I -> A, (int A) {if (A > 1I) print(A);endif;}(4I)-> B);
    SUMAR(4I -> A, (int A) if (A > 1I) print(A);endif;(A) -> B);
    SUMAR(4I -> A, (int A) if (A > 1I) print(A);endif;}(A) -> B);
    SUMAR(4I -> A, (int A) {if (A > 1I) print(A);endif;(A) -> B);
    
}