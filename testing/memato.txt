P{
    @int A.B; no se si se debria poder declarar asi
    int A, B, C;
    @int A B, C; @/ no reconoce que falta la coma, (la regla está comentada) porque si no tengo shift reduce
    int A, B, ;
    int A, B
    int A
    int FUNCION (int A) {}
    int FUNCION (int A, cv int B) {}
    int  (int A, cv int B) {}
    int FUNCION (int A, int B) {}
    int A

    A := B;
    A := 3I;
    A := 4.5;
    A := 4.5D+6;
    A.C := 3I + 4I;
    A.C := 3I - 4I;
    A := 4 * 6.6;
    A := 4 / 6.6;
    A.C := 3I + ;
    A.C := 3I - ;
    A := 4 * ;
    A := 5 / ;
    A.C := + 3I ;
    A.C := - 3I ;
    A.C := - -3I ;
    A :=  * 4;
    A := / 5 ;
    A := 4I 5I;
    @ A := B 3I; @/ Comentado porque se rompe

    A.A = 1;

    A, B = 1;
    A.A, B.B, C.C = 1I, 2I, 3I;

    A, B = ;
    
    = 2I, 3I;

    A B = 2I, 3I;
    A, B = 2I 3I;

    A := 3I;

    if (A > 5I) {
        A := 3I + 3I;
    } else {
        B := 3I * 2;
    }
    endif;

    if (A < 5I) {
        A := 5;
    }
    endif;

    if (A == 5)
        A := 3I;
    endif
    
    if A == 5)
        A := 3I;
    endif;
    
    if (A == 5
        A := 3I;
    endif;
    
    if A == 5
        A := 3I;
    endif;

    if (A =! 5)
    else
    endif;

    if (A =! 5) {

    } else {
        
    }
    endif;

    if A =! 5) {

    } else {
        
    }
    endif;

    if (A =! 5 {

    } else {
        
    }
    endif;

    if A =! 5 {

    } else {
        
    }
    endif;

    if () {

    }
    endif;

    if (A >) {

    }
    endif;

    if ( < B) {

    }
    endif;

    for (I from 1I to 10I) {
        B := A;
        A := A + 1;
    };

    for (I from 1I to 10I)
        A := A + 1;

    for I from 1I to 10I) {
        A := A + 1;
    };

    for ( from 1I to 10I) {
        A := A + 1;
    };

    for (I 1I to 10I) {
        A := A + 1;
    };

    for (I from to 10I) {
        A := A + 1;
    };

    for (I from 1I  10I) {
        A := A + 1;
    };

    for (I from 1I to ) {
        A := A + 1;
    }

    for (I from 1I to 10I {
        A := A + 1;
    }

    for I from 1I to 10I {
        A := A + 1;
    }

    for (I from 1I to 10I) {
        
    }
    
    for (I from 1I to 10I) 

    print(8.5);
    print(8.5)
    print8.5);
    print();
    print(8.5;
    
}